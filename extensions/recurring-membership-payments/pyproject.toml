[project]
authors = [
  {email = "engineering@canvasmedical.com", name = "Canvas Team"}
]
dependencies = [
  "canvas[test-utils]"
]
description = "Monthly membership subscriptions through Pay Theory for Apex."
license = "MIT"
name = "recurring-membership-payments"
readme = "apex_recurring_membership_payments/README.md"
requires-python = ">=3.11"
version = "0.1.0"

[tool.pytest.ini_options]
python_files = ["*_tests.py", "test_*.py", "tests.py"]

[dependency-groups]
dev = [
  "pytest-cov"
]
